# Marktlokation informative data (SEQ+Z98) for PID 55002.
# Merges into entity "Marktlokation" via deep-merge.
#
# SEQ+Z98  (Informative Daten der Marktlokation)
#
# Note: PID 55002's SEQ+Z98 contains only the qualifier — no C286 data.
# All fields are defaults so the engine always generates this entry segment
# (has_data_fields=false → phantom prevention is skipped).

[meta]
entity = "Marktlokation"
bo4e_type = "Marktlokation"
companion_type = "MarktlokationEdifact"
source_group = "SG4.SG8"
source_path = "sg4.sg8_z98"
discriminator = "SEQ.d1229=Z98"

[fields]

[fields."seq.d1229"]
target = ""
default = "Z98"
